@model ImageResizer.Models.Image

@{
    ViewBag.Title = "View";
}


<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
        </dt>

        <dd>
            <img src="@Url.Content(Model.ImagePath)" />
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Resize", "Resize", new { id = Model.ImageId }) |
    @Html.ActionLink("Convert", "Convert", new { id = Model.ImageId }) |
    @Html.ActionLink("Do Absolutely Nothing","Add")
</p>
